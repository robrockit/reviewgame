// Import jest-dom matchers and their TypeScript type declarations for vitest.
// This single import extends expect() at runtime AND augments the Vitest
// Assertion interface so TypeScript recognises matchers like toBeInTheDocument.
import '@testing-library/jest-dom/vitest';
